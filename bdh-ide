#!/bin/bash

# 1. Safety Check (இது மட்டும் bash-ல் இருக்கட்டும்)
if [ -n "$TMUX" ]; then exit 1; fi

# 2. தமிழி மொழியை இயக்குதல் (மற்ற அனைத்து வேலைகளும் இதனுள்)
tamizhi run /etc/bdh-ide/ide_layout.tz

# 3. இறுதியாக செஷனை இணைத்தல்
tmux attach -t archide
